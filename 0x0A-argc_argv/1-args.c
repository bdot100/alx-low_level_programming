#include <stdio.h>

/**
  * main - Prints the no of args passed into the program
  * @argc: argument count
  * @argv: argument vector
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%d\n", argc);

	return (0);
}
